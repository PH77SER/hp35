class MergeSort
{
    public int[] sort(int[] array1)
    {
        
        int[] array2 = new int[array1.Length]; //could be half tbh
        for (int i = 0 ; i < array1.Length/2 ; i++)
            array2[i] = array1[i]; //array2 now has the first half of array1
        
        
    }
}